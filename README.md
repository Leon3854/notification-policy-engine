
# Движок политик уведомлений

Это полноценный боевой микросервис: 
> работа с часовыми поясами (таймзоны),<br/> 
> глобальные политики блокировок,<br/> 
> логирование (Observability), <br/>
> идемпотентность и полное покрытие тестами.<br/>
	
**Главный фокус — на <u>качестве архитектуры</u>, типов, бизнес‑логики... Docker приветствуется... Форматы можно менять.**


## В чем суть задания? - В таймзонах 

Надо решить проблему часовых поясов для того чтобы пользователь живущий в Париже или для жителя Москвы сообщение приходло с учетом времени по его региону. Простыми словами сообщения должны приходить с учетом часового пояса, в разрешенное время.

Логика что была исользована в самом сервисе имее большой потенциал в работе ее можно будет использовать в различных сервисах где нужны будут уведомления. Прекрасно подойдет для платежного сервиса да и не только.

**В данной работе будет разработка только backend стороны без работ на клиентской стороне.**

## Запуск сервиса:

1. В корне проекта находится файл окружения переименуйте его **.yourEnv** в **.env** и пропишите свои вводные
2. ```bash
   docker-compose up -d --build
   ```
   Для чего эта команда: Для запуска всего приложения через одну команду
   
   **результат выполнения команды из корня репозитрия**

   <p align="center">
  		<img src="./root-dock.png" alt="Результат выполнения команды из корня" width="600"/>
   </p>

2.1 Команду 
```bash
docker-compose logs -f policy-engine
```
**не вводить будет ошибка**: причина проста надо настраивать Prisma7 - очень капризная от проекта к проетку не бывает одинаоковой настройки при использовании Docker каждый раз "танцы с бубном" пока локальный запуск(нет времени и так закапался)


3. ```bash
   npm run start:dev
   ```
   Запуск сервиса локально, только самого сервиса без запуска Docker
   
   **результат выполненя локальной команды из корня сервиса**
   
   <p align="center">
  		<img src="./lockal-service.png" alt="Результат выполнения команды из корня сервиса" width="600"/>
   </p>

4. **Запуск Unit-тестов**

   ```bash
	npm test
	```

4.1 **Запустить в watch-режиме**
```bash
npm run test:watch
```

4.2 **Запустить конкретный файл**
```bash
npx jest src/message/message.service.spec.ts
```

**результат выполненя локальной команды из корня сервиса по тестированию**
   
   <p align="center">
  		<img src="./tests-unit.png" alt="Результат выполнения команды из корня сервиса" width="600"/>
   </p>
Красная строка в логах — это не ошибка теста, а ожидаемый лог от одного из тестов, который специально проверяет обработку некорректной таймзоны:
<br/>

> ERROR [MessageService] Ошибка парсинга таймзоны для региона Invalid/Timezone
<br/>
Это тест "Должен обработать ошибку таймзоны и очистить ключ идемпотентности" — он специально передаёт Invalid/Timezone и проверяет, что сервис корректно обрабатывает эту ошибку. Всё работает как задумано!
<br/>

> Test Suites: 3 passed, 3 total<br/>
> Tests:       15 passed, 15 total<br/>


### Что, доработать (на будущее):

**1. Дефолтные настройки для новых пользователей**
В коде `message.service.ts` для нового пользователя задаётся жёсткий объект с константами.
Было бы не плохо вообще такой объект использовать только как одно из условий и отработать определение по геопозиции пользователя его часовой пояс сразу по умолчанию. 
*  Завести отдельную таблицу/модель `DefaultPreferences` в БД и при создании пользователя копировать их. Это сделает систему гибче — администратор сможет менять дефолты без перевыпуска кода.

**2. Идемпотентность команд изменения настроек**
Идемпотентность реализована через Redis для метода `evaluate`- это защита от дублей запроса. Надо будет проверить и улучшить. Возможно надо добавить новые условия и проверить не вызывает ли ошибки и просто возвращает текущее состояние.

